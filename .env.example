# Database
# PostgreSQL connection string (Supabase, Neon, Railway, or local)
DATABASE_URL="postgresql://user:Nawsaafa_12600uu!@host:5432/database?schema=public"

# Optional: Direct connection for migrations (bypasses connection pooler)
DIRECT_URL="postgresql://user:Nawsaafa_12600uu!@host:5432/database?schema=public"

# Application
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Future: Cloud storage for media (showreels, photos)
# CLOUDINARY_URL=""
# AWS_S3_BUCKET=""

# Authentication (NextAuth v5)
# Generate with: openssl rand -base64 32
AUTH_SECRET=
# Optional: Override callback URL (defaults to NEXT_PUBLIC_APP_URL)
# AUTH_URL="http://localhost:3000"

# Stripe Payment Processing
# Get keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
# Get webhook secret from Stripe CLI: stripe listen --forward-to localhost:3000/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET="whsec_..."

# Stripe Price IDs (create in Stripe Dashboard or leave empty to auto-create)
# STRIPE_PRICE_TALENT_MEMBERSHIP="price_..."
# STRIPE_PRICE_PROFESSIONAL_ACCESS="price_..."
# STRIPE_PRICE_COMPANY_ACCESS="price_..."
